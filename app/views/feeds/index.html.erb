<h1>Feeds#index</h1>

<div id='add-feed'>
	<%= render partial: 'new_feed_form' %>
</div>

<div id='posts'>
	<% @posts.each do |post| %>
		<p><%= post.title %></p>
	<% end %>
</div>

<div id='add-feed'>
	<%= render :partial => 'feeds/new_category_form' %>
</div>

<% @posts.each do |post| %>
	<p><%= post.title %></p>
<% end %>

