<h2>Tag this feed</h2>

<%= form_for :tag, url: new_tag_path do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.submit value: "Tag feed" %>
	<%= f.hidden_field :feed, :value => params[:feed] %>

<% end %>